---
output: 
  pdf_document:
    latex_engine: xelatex
mainfont: "Arial"
params:
  county_attributes: NULL
---

\includegraphics[width=0.3\textwidth]{logo_rect.png}

\begin{center}
  {\LARGE `r params$county_attributes$Value[params$county_attributes$Attribute == "County Name"]` County, `r params$county_attributes$Value[params$county_attributes$Attribute == "State Name"]`}
\end{center}

\begin{center}
  \large \today
\end{center}

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r nriTable, echo=FALSE}
# if (!is.null(params$county_attributes)) {
#   df <- params$county_attributes
#   knitr::kable(t(df))  # try vertical table
# }
if (!is.null(params$county_attributes)) {
  knitr::kable(params$county_attributes, col.names = c("Attribute", "Value"))
}
```

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```
